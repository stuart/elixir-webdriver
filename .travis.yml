language: elixir
elixir: 0.15.1
notifications:
  recipients:
    - stuart.coyle@gmail.com
otp_release:
  - 17.0
env:
  - MIX_ENV=test

before_install:
   - "export DISPLAY=:99.0"
   - "sh -e /etc/init.d/xvfb start"

script: "mix do deps.get, deps.compile, test"
